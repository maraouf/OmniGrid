{
  "name": "omnigrid-vendor",
  "version": "1.1.0",
  "private": true,
  "description": "Dev-only tooling. Manages the front-end dependencies (alpinejs, sweetalert2, @tailwindcss/browser). The server never runs npm — it just serves node_modules/ directly via FastAPI's StaticFiles mount at /node_modules/.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@tailwindcss/browser": "*",
    "@xterm/addon-fit": "*",
    "@xterm/addon-web-links": "*",
    "@xterm/xterm": "*",
    "alpinejs": "^3.15.12",
    "qrcode-generator": "*",
    "sweetalert2": "*"
  }
}
